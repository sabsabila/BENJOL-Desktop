<Page:MyPage xmlns:Page="clr-namespace:Velacro.UIElements.Basic;assembly=Velacro"
      x:Class="TestWPPL.Payment.PaymentPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TestWPPL.Payment"
      mc:Ignorable="d" 
      FontFamily="{StaticResource Muli}"
      Title="PaymentPage">

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#FFF0F0F7">
        <Label Content="Invoices" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Foreground="#FF636363" FontSize="24"/>
        <ListView x:Name="paymentList" HorizontalAlignment="Stretch" Height="Auto" Margin="20,70,20,40" VerticalAlignment="Stretch" Width="Auto">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding payment_id}" Header="#" Width="40"/>
                    <GridViewColumn DisplayMemberBinding="{Binding first_name}" Header="Name" Width="100"/>
                    <GridViewColumn DisplayMemberBinding="{Binding service_name}" Header="Service" Width="120"/>
                    <GridViewColumn DisplayMemberBinding="{Binding service_cost}" Header="Cost" Width="100"/>
                    <GridViewColumn DisplayMemberBinding="{Binding bengkel_note}" Header="Note" Width="200"/>
                    <GridViewColumn DisplayMemberBinding="{Binding status}" Header="Status" Width="110"/>
                    <GridViewColumn  Header="Action" Width="auto">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Style="{StaticResource MaterialDesignFlatLightBgButton }" x:Name="updateButton" HorizontalAlignment="Right" Background="#FFFCCA53" Foreground="#FF636363" Content="Update" Click="onUpdateStatusPaymentBtn_Click"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page:MyPage>
